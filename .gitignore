node_modules
dist
__pycache__/
.DS_Store
*.local
.claude/worktrees/
/shots/
*.tmp.mjs

# Anything baked out of the client.  The extractor is committed; its output is
# not, and the reason is the wiki page "저작권과 배포 경계": bytes that came out
# of Blizzard's archives do not enter this repository whatever shape they are
# in by the time they leave the pipeline.
/public/data/
